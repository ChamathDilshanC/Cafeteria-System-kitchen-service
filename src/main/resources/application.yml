spring:
  application:
    name: kitchen-service # Must match the filename in config-server/config/
  config:
    import: "configserver:${CONFIG_SERVER_URI:http://localhost:9000}"
  cloud:
    config:
      fail-fast: true

